# Production Environment

This directory is prepared for future use. When production environment is ready, add environment-specific values files here following the same pattern as the `dev` directory.

## Planned Structure

- `values-auth.yaml`: Overrides for auth service in production
- `values-gestao-de-alunos.yaml`: Overrides for gestao-de-alunos service in production
- `values-gestao-de-professores.yaml`: Overrides for gestao-de-professores service in production









