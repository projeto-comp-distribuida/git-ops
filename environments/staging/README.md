# Staging Environment

This directory is prepared for future use. When staging environment is ready, add environment-specific values files here following the same pattern as the `dev` directory.

## Planned Structure

- `values-auth.yaml`: Overrides for auth service in staging
- `values-gestao-de-alunos.yaml`: Overrides for gestao-de-alunos service in staging
- `values-gestao-de-professores.yaml`: Overrides for gestao-de-professores service in staging







