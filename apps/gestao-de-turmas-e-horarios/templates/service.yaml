apiVersion: v1
kind: Service
metadata:
  name: {{ include "gestao-de-turmas-e-horarios.fullname" . }}
  labels:
    {{- include "gestao-de-turmas-e-horarios.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gestao-de-turmas-e-horarios.selectorLabels" . | nindent 4 }}






















