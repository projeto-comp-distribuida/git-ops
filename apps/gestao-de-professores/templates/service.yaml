apiVersion: v1
kind: Service
metadata:
  name: {{ include "gestao-de-professores.fullname" . }}
  labels:
    {{- include "gestao-de-professores.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gestao-de-professores.selectorLabels" . | nindent 4 }}

