{{- if .Values.sealedSecrets.enabled }}
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: {{ include "gestao-de-professores.fullname" . }}-secrets
  labels:
    {{- include "gestao-de-professores.labels" . | nindent 4 }}
spec:
  encryptedData:
    {{- range $key, $value := .Values.sealedSecrets.data }}
    {{ $key }}: {{ $value }}
    {{- end }}
  template:
    metadata:
      name: {{ .Values.sealedSecrets.secretName | default (printf "%s-secrets" (include "gestao-de-professores.fullname" .)) }}
      labels:
        {{- include "gestao-de-professores.labels" . | nindent 8 }}
    type: Opaque
{{- end }}

