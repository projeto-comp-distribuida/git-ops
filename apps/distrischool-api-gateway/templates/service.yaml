apiVersion: v1
kind: Service
metadata:
  name: {{ include "distrischool-api-gateway.fullname" . }}
  labels:
    {{- include "distrischool-api-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "distrischool-api-gateway.selectorLabels" . | nindent 4 }}






















