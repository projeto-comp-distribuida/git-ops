apiVersion: v1
kind: Service
metadata:
  name: {{ include "gestao-de-notas.fullname" . }}
  labels:
    {{- include "gestao-de-notas.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gestao-de-notas.selectorLabels" . | nindent 4 }}






















