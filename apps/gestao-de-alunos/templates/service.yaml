apiVersion: v1
kind: Service
metadata:
  name: {{ include "gestao-de-alunos.fullname" . }}
  labels:
    {{- include "gestao-de-alunos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gestao-de-alunos.selectorLabels" . | nindent 4 }}









